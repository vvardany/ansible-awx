---
  - name: Show facts
    debug:
    msg: "{{ inventory_hostname }} ### {{ ansible_play_hosts[-1] }} ### {{ ansible_play_hosts[0] }} ### {{ ansible_play_hosts[1] }}"

  - name: testing inventory hostname
    shell: echo Hello
    when: inventory_hostname == ansible_play_hosts[1]
